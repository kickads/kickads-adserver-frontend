version: '3'
services:
  kickads-adserver-frontend:
    container_name: kickads-adserver-container
    build:
      dockerfile: ./Dockerfile
    image: kickads-adserver-frontend-image
    ports:
      - 3000:3000
    volumes:
      - .:/app
      - /app/node_modules/
    command: pnpm dev